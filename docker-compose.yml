version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: mf_user
      POSTGRES_PASSWORD: mf_password
      POSTGRES_DB: mf_analytics
    ports:
      - "5432:5432"
  
  redis:
    image: redis:7
    ports:
      - "6379:6379"